# CI Config
cache:
  paths:
  - node_modules/
  - src/assets/bower_components

stages:
  - deploy

# Deploy to Production(S3)
deploy_prod:
  stage: deploy
  image: node:latest
  only:
    - master
  script:
    - grunt deploy
